# A private declaration went, and building it was work the module did on evaluation.
# Nothing outside the file could name it, so the export table is what it always was;
# what an outsider can tell is that the work is no longer done, which is module
# initialisation. Compare `renamed-export`, where the name was exported and the name
# itself carries the mark.
[[anchor]]
path = "src/pages/DatePage.tsx"
affected = true

[anchor.explain]
base = [
  "File(src/pages/DatePage.tsx)",
  "ModuleInit(src/pages/DatePage.tsx)",
  "ModuleInit(src/utils/helpers.ts)",
]
