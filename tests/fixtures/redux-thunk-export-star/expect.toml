# The store's `createAsyncThunk` arrives through an `export *` barrel. It is still
# `withTypes` of RTK's, so creating the thunk runs nothing, and a case built from
# the slice's reducer does not reach the edit to the payload creator.
[[anchor]]
path = "src/cases/plan-badge.cases.tsx"
affected = false
