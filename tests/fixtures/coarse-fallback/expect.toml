# A CommonJS export table coarsens the whole module, so even a change to an unrelated
# declaration flags. Giving up always means "treat this as one opaque unit", never
# "not affected".
[[anchor]]
path = "src/pages/LegacyPage.tsx"
affected = true
